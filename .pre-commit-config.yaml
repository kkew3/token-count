repos:
  - repo: local
    hooks:
      - id: update_available_models
        name: Update models list and type hints
        entry: python3 scripts/update_available_models.py
        language: system
        always_run: true
        pass_filenames: false
